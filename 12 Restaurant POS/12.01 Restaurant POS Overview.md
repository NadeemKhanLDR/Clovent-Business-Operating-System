# Restaurant POS — Item Cart & Order Entry Hardening

## A. Feature Overview
**Module:** Restaurant POS (Clovent Business Operating System)  
**Milestone:** Item Cart & Order Entry Workflow Hardening  
**Target Architecture:** .NET 10 / Windows Forms / DevExpress 26.1 / SQL Server  

This document defines the hardened implementation, domain business rules, technical architecture, and verification state for the Restaurant POS Item Cart and Order-Entry workflow.

---

## B. Business Rules

### 1. Item Addition & Variant Naming
- **Single Variant Products:** Clicking a menu item adds 1x Standard portion variant to the cart.
- **Multi-Variant Products (Full / Half):** Each variant displays as `[Product Name] - [Portion]` (e.g. `Aloo Gobi - Full`, `Aloo Gobi - Half`).
- **Naming Consistency:** The `Product Name - Portion` naming format is used consistently across Cart, Hold, Recall, Active Orders, Receipts, Payment History, and Sales History.
- **Portion Normalization:** "Regular" or default portions render as `Product Name` without redundant portion suffixes.

### 2. Duplicate Item Behavior
- **Same Item + Same Variant + No Notes:** Merges into a single line with incremented quantity (`Quantity = Quantity + addedQty`).
- **Same Item + Same Variant + Custom Notes:** Maintained as separate lines to ensure distinct kitchen modifiers (e.g., `Note: Less spicy` vs. `Note: Extra spicy`) do not overwrite one another.

### 3. Quantity Controls
- **Validation:** Quantity must be a positive integer ($\ge 1$).
- **Controls:** `+` (increment), `-` (decrement), and `Qty` button / inline cell editor.
- **Immediate Recalculation:** Changing quantity immediately recalculates Subtotal, Tax, Discount, Service Charge, Total Payable, and Balance Due across all UI controls and domain models.

### 4. Notes Persistence
- **Item Notes (`OrderLine.Notes`):** Attached per cart line. Persisted in database and preserved across `MENU -> CART -> HOLD -> RECALL -> CART -> COMPLETE`.
- **Order Notes (`Order.Notes`):** Free-text internal notes attached to the order.
- **Customer Notes (`Order.CustomerNotes`):** Customer-facing notes printed on receipts.

### 5. Delete vs. Void Semantics
- **Delete (`RemoveOrderLineCommand`):** Removes uncommitted lines from an active working order before payment/kitchen processing.
- **Void (`VoidOrderLineCommand` / `VoidOrderCommand`):** Audit operation. Sets `IsVoided = true`. Voided lines and orders are excluded from financial totals and kitchen tickets but preserved permanently in the database for audit trails.
- **Manager Authorization:** Full order voiding requires reason prompt and Manager Authorization (`pos.void` permission).

### 6. Price Override
- **Catalog Snapshot:** `OriginalUnitPrice` preserves the catalog price at creation.
- **Price Edit (`OverrideOrderLinePriceCommand`):** Authorized via `pos.priceoverride` permission (button or inline grid editor). Requires a reason and records the cashier/manager user.
- **Persistence:** Overridden prices persist through Hold, Recall, Receipt printing, and Sales History.

### 7. Authoritative Total Calculation
All money totals are computed by `OrderTotalsCalculator` via a single, authoritative path:
$$\text{Subtotal} = \sum (\text{Quantity} \times \text{UnitPrice} \quad \forall \text{ active lines})$$
$$\text{Grand Total} = \text{Subtotal} - \text{DiscountTotal} + \text{ServiceChargeTotal} + \text{ExclusiveTaxTotal}$$
$$\text{Balance Due} = \text{Grand Total} - \text{PaidTotal}$$

### 8. Balance Due Behavior
- **Dynamic Balance:** `Balance Due` equals `Grand Total` minus `PaidTotal` (non-voided payments).
- **Partial Payments:** Subtotal and Grand Total remain fixed while Balance Due decreases as partial payments are recorded.

### 9. Hold & Recall Integration
- **Hold Order (`HoldOrderCommand`):** Transitions order status to `Held`. Preserves order ID, number, items, variants, quantities, notes, prices, customer, and table. Clears the working cart UI safely without deleting persisted orders.
- **Recall Order (`RecallOrderDialog`):** Restores held orders back into the POS workspace with 100% data fidelity.
- **Safety Checks:** Switching orders or recalling a held order when active unsaved edits exist prompts the cashier to hold the current order first.

### 10. Clear Working Cart
- **Clear Action (`ClearOrderAsync`):** Safely resets the UI workspace (`_currentOrder = null`, `_currentOrderLines = []`) without deleting or cancelling held or open orders in the database.

### 11. Dine In vs. Take Away
- **Table Selection = Dine-In Intent (2026-09-15):** Selecting a valid table in the Table dropdown when no working order exists automatically starts a Dine-In order (`CreateOrderCommand(OrderType.DineIn, warehouse, tableId)`) seated at that table. The cashier can add items immediately — **no separate "+ Dine In" click exists any more** (the button was removed from the header; `+ Take Away` remains). The decision is centralized in `TableSelectionDineInPolicy` (`src/Clovent.Desktop/Restaurant/Orders/TableSelectionDineInPolicy.cs`).
- **No Table:** Selecting "(No Table)" never creates an order by itself.
- **Table with a live order:** Selecting a table that already seats an Open/Held order surfaces that order (resuming it if Held) instead of creating a duplicate; the existing hold-before-switch prompt and transfer/occupancy rules apply unchanged (`TransferOrderTableCommand`, `GetOpenOrHeldOrderByTableQuery`).
- **Changing table before items:** With no working order, selecting T-01 then T-02 results in exactly one order, transferred to T-02 — no duplicate orders are created.
- **Dine In:** Requires a valid table (`TableId`). Table selector control is enabled.
- **Take Away:** `TableId` is `null`. Table selector control is disabled and unselected — a Take Away order can never pick up a table.

### 12. Customer & Table Association
- **Default Customer Architecture:** The default customer is a real, persisted `Customer` entity in `[Restaurant].[Customers]` (seeded as `C000`, `Walk-in Customer`, `IsDefault = 1`, `IsActive = 1`) rather than an in-memory pseudo-customer or null entity.
- **Auto-Selection on New Order:** When a new order is initiated (automatic Dine-In via table selection, or `+ Take Away`), the POS automatically assigns and selects the active default customer record. The cashier may change the customer at any time without altering system default settings.
- **Single Active Default Customer Invariant:** Only ONE customer record across the entire system may have `IsDefault = true` at any given time. Designating a new default customer automatically clears the default flag from the previous default. Only an active customer (`IsActive == true`) may be designated as default; deactivating a default customer drops its default status automatically.
- **Customer Configuration in Back Office:** Configurable in the Customers management screen (`CustomersView`) via the edit form checkbox (`Default Customer`), a dedicated "Default" column (`colDefault`), and a "Set as Default" toolbar button (`_btnSetDefault`).
- **Customer Lookup Popup:** Displays user-facing customer columns: `Customer Code`, `Name`, `Phone`, and `Balance`. The internal database GUID (`CustomerId`) is strictly hidden from the popup UI while remaining the underlying `ValueMember` key for selection, command dispatch, and database persistence.
- **Table Association & Lifecycle:** Occupied tables link to `Order.TableId` and reflect `TableOccupancyStatus.Occupied`. The table is immediately vacated back to `TableOccupancyStatus.Available` when an order is completed, cancelled, or voided.
- **Table Code Uniqueness:** Tables are strictly unique by code within each dining area via unique index `IX_Tables_DiningAreaId_Code` and validated in `CreateTableCommandHandler`. Duplicate codes cannot be created.
- **Table Occupancy Self-Healing Reconciliation:** To prevent orphaned or drifted `Occupied` table statuses (e.g. from abandoned orders or legacy state), `ReconcileTableOccupancyCommand` runs on application startup (`TableOccupancyReconciliationStartupTask`) and during table list queries (`ListAllTablesQueryHandler`, `ListTablesByDiningAreaQueryHandler`). The reconciliation verifies against real active orders (`Open` or `Held`), safely vacating tables that have no active orders without resetting valid occupied tables or disturbing `OutOfService` / `Reserved` states.

---

## C. Order Lifecycle Workflow

$$\text{NEW} \longrightarrow \text{ADD ITEMS} \longrightarrow \text{MODIFY CART} \longrightarrow \text{HOLD} \longrightarrow \text{CLEAR UI} \longrightarrow \text{RECALL} \longrightarrow \text{RECORD PAYMENT} \longrightarrow \text{COMPLETE}$$

- **Open:** Active order receiving item additions or modifications.
- **Held:** Persisted in database, available for recall.
- **Completed:** Fully paid ($\text{Balance} \le 0.005$), inventory stock issued, table vacated.
- **Voided:** Discarded with audit reason and manager authorization; table vacated.
- **Cancelled:** Cancelled before any payment has been recorded; table vacated.

---

## D. Technical Implementation

### Key Components Changed/Hardened
1. **`RestaurantPosForm.cs`**:
   - Central POS desktop form.
   - Dual grid/list menu viewports with responsive layout engine (1024x768 and 1366x768).
   - In-place grid editing for Quantity and Price with permission gates.
   - Active Orders collapsible rail with status filters (`Active Orders`, `Take Away`, `Closed`, `Wait List`).
   - Integrated tender strip, keypad, quick cash, and action buttons.
   - Shift management integration: queries active cashier shift via `GetActiveShiftQuery` and associates `ShiftId` with payments.
   - Re-entry protection on `RecordButton_Click` and cancellation guards on `EnsureShiftActiveOrPromptAsync`.
   - **Suggested Add-ons Popup**: Native DevExpress `PopupContainerEdit` + `PopupContainerControl` popup displaying cart cross-sell recommendations. Features lightbulb icon (`actions_idea.svg`) docked immediately to the left of the header title `"Suggested Add-ons"`. Fixed checkbox column (38px), dominant Item column (min 180px, auto-fills available width to prevent text clipping), fixed Portion column (75px), and fixed right-aligned Price column (85px), with popup width scaled to 430px. Clicking the bulb icon or title immediately opens the dropdown.
2. **`RestaurantPersistenceInitializer.cs`**:
   - Idempotently verifies and creates `[Restaurant].[Shifts]` and `[Restaurant].[CashMovements]` tables, indexes, and `ShiftId` foreign keys at application startup.
   - Deduplicates legacy duplicate tables in the database and applies `IX_Tables_DiningAreaId_Code` unique index on `[Restaurant].[Tables]([DiningAreaId], [Code])`.
3. **`TableOccupancyReconciliationStartupTask.cs` & `ReconcileTableOccupancyCommand.cs`**:
   - Background startup task and query-time reconciler ensuring table occupancy states accurately match active orders without falsely vacating occupied tables or disturbing out-of-service tables.
4. **`GuardedAction.cs` & `ErrorDialogForm.cs`**:
   - Exception-resilient action execution with friendly summaries and functional "Show Details" error reporting.
5. **`OrderTotalsCalculator.cs`**:
   - Pure domain calculation engine for Subtotal, Tax, Discount, Service Charge, Grand Total, Paid Total, and Balance.
6. **`Order.cs` & `OrderLine.cs`**:
   - Aggregate roots managing order transitions and line item immutability/overrides.
7. **`RecallOrderDialog.cs`**:
   - Browse and recall held/open orders with search, preview, and stale-data re-query guards.
8. **POS Startup Determinism, Lifecycle Management & Window Lifetime Hardening (`RestaurantPosForm.cs`)**:
   - **Global Splash Elimination:** Removed global top-level `SplashScreenManager` wait form from POS navigation and startup. The login screen explicitly closes the splash (`_splashScreenService.Close()`) before displaying POS, preventing lingering top-level windows from masking UI elements or freezing input.
   - **Elimination of Blocking White Overlay:** Replaced the legacy blocking full-screen white loading overlay with non-blocking asynchronous status updates in the header order status label (`_orderStatusLabel`) and responsive cursor feedback, ensuring the real POS shell renders immediately without a 10–15s skeleton freeze.
   - **Window Lifetime Ownership:** Window lifetime is strictly governed by `CbosApplicationContext` and `IApplicationModeNavigator`. `RestaurantPosForm.OnFormClosed` no longer unconditionally invokes `ExitApplication()`. Mode transitions (`ModeSwitchToBackOffice`, `StartupFailureFallback`) cleanly switch top-level active forms without killing the process message loop. Only explicit user closure ('X' button) triggers application exit.
   - **Re-Entrancy Gate:** `MainForm.OpenRestaurantPosWithGateAsync` enforces an atomic `_isOpeningPos` re-entrancy lock preventing duplicate form instantiation or corrupted session state under rapid click conditions.
   - **Graceful Startup Failure Recovery:** When database queries fail during initialization, `HandleStartupFailureAsync` prompts the user with Retry/Cancel options. Canceling safely navigates back to the Back Office shell rather than terminating CBOS or leaving an orphaned headless process.
   - **Structured Lifecycle Telemetry:** End-to-end lifecycle milestones are logged: `POS_FORM_CONSTRUCTED`, `POS_FORM_LOAD`, `POS_INIT_STARTED`, `POS_INIT_01..08`, `POS_INIT_READY`, `POS_INIT_FAILED`, `POS_FORM_CLOSING`, `POS_FORM_CLOSED`, `NAVIGATOR_MODE_SWITCH_*`, and `APPLICATION_EXIT_REQUESTED`.
   - **Resilient Currency Formatting:** `CurrencyDisplayLoader` wraps database queries in defensive exception handling with safe defaults (`Rs.`, 2 decimals) so network/database glitches never abort POS initialization.
9. **Visual Studio Designer Compatibility & Layout Hardening**:
   - **Procedural Code Eviction:** Cleaned `CustomerLedgerDialog.Designer.cs` and `EndOfDayReportView.Designer.cs` of C# tuple deconstructions and procedural loops (`ReportPeriodCalculator.GetAllOptions()`), moving runtime sync to `ConfigureReportPeriodOptions()` called during form load.
   - **DPI-Aware Form & Button Metrics:** Configured explicit `MinimumSize` (85x28) and non-zero button dimensions on `MasterDataEditFormBase.Designer.cs` and `CustomerEditForm.Designer.cs`, preventing button squishing and label clipping under 96 DPI and 250% high-DPI scaling environments.

---

## E. Manual Acceptance Validation

### Validation Overview
- **Validation Date:** 2026-09-11  
- **Scope:** 30 Core Restaurant POS Desktop Workflows  
- **Environment:** Windows / .NET 10 / DevExpress 26.1 / SQL Server (`Clovent_Restaurant`)  
- **Manual Acceptance Status:** **PASS**  

> [!IMPORTANT]
> Manual acceptance testing was performed by the developer/user against the actual running Restaurant POS desktop application, and all tested workflows listed below were confirmed working. Automated unit and integration tests (.NET test suite) validate domain rules, application commands, and persistence infrastructure separately.

### Tested & User-Verified Workflows

| # | Workflow / Scope | Result | Validation Summary |
|---|---|---|---|
| 1 | **Login** | **MANUAL PASS** | Development login and Admin login work cleanly. |
| 2 | **New Order** | **MANUAL PASS** | New Take Away order creation works correctly. |
| 3 | **Product / Variant** | **MANUAL PASS** | Products and portion variants (e.g. *Aloo Gobi - Half*, *Aloo Gobi - Full*) add correctly with consistent naming. |
| 4 | **Quantity** | **MANUAL PASS** | Quantity increment/decrement works; quantity cannot drop below 1; totals recalculate immediately. |
| 5 | **Notes** | **MANUAL PASS** | Item notes work and remain associated with the correct order line throughout the order lifecycle. |
| 6 | **Duplicate behavior** | **MANUAL PASS** | Same product + variant without custom notes merges quantity; custom notes preserve separate cart lines. |
| 7 | **Delete** | **MANUAL PASS** | Delete removes uncommitted working cart lines correctly. |
| 8 | **Void** | **MANUAL PASS** | Void sets `IsVoided = true`, preserves audit trail/reasons, and excludes amounts from financial totals. |
| 9 | **Customer** | **MANUAL PASS** | Customer association works and customer information persists with the order across Hold/Recall. |
| 10 | **Hold / Recall** | **MANUAL PASS** | Order can be held and recalled with 100% data fidelity (items, variants, quantities, notes, prices, customer, table). |
| 11 | **Clear after Recall** | **MANUAL PASS** | Clear resets the working cart UI workspace without deleting the underlying saved/held order in the database. |
| 12 | **Dine-In** | **MANUAL PASS** | Dine-In order workflow works; selecting a table automatically starts the Dine-In order (no extra "+ Dine In" click) and associates the table correctly. |
| 13 | **Take Away** | **MANUAL PASS** | Take Away workflow works; table selection requirement is correctly disabled/bypassed. |
| 14 | **Active Orders** | **MANUAL PASS** | Active Orders sidebar and filters work; order cards display correctly; no ghost 0-item/0.00 active orders exist. |
| 15 | **Active Orders visibility** | **MANUAL PASS** | Show/Hide animation works; no unacceptable blinking or flickering was observed during manual acceptance. |
| 16 | **Cancel Order** | **MANUAL PASS** | Cancel Order workflow works; confirmation prompt and reason entry function correctly. |
| 17 | **Payment Method** | **MANUAL PASS** | Payment Method dropdown works; default payment preference loads as configured. |
| 18 | **Amount Tendered** | **MANUAL PASS** | Amount Tendered input works; Exact button works; change calculation and Balance Due display accurately. |
| 19 | **Numeric Keypad** | **MANUAL PASS** | Numeric keypad digits (0-9), decimal, backspace, and clear buttons work. |
| 20 | **Quick Cash** | **MANUAL PASS** | Quick Cash buttons (100, 200, 500, 1000, 2000, 5000) update tender field correctly. |
| 21 | **Record Payment** | **MANUAL PASS** | Record Payment records payment correctly, updates paid total/balance due, and prevents duplicate payments. |
| 22 | **Split Payment** | **MANUAL PASS** | Split Payment workflow allocates balance across multiple payment methods correctly. |
| 23 | **Print Bill** | **MANUAL PASS** | Print Bill receipt preview workflow displays full order breakdown correctly. |
| 24 | **Place Order** | **MANUAL PASS** | Place Order completes transaction, issues stock, vacates table, and transitions order to Completed. |
| 25 | **Closed / Sales History** | **MANUAL PASS** | Completed orders transition correctly and appear in Sales History / Closed tab. |
| 26 | **Restart persistence** | **MANUAL PASS** | Application restart does not corrupt persisted order data; saved and completed orders remain available. |
| 27 | **Resolution 1024x768** | **MANUAL PASS** | POS layout manually verified usable without clipping at 1024x768. |
| 28 | **Resolution 1366x768** | **MANUAL PASS** | POS layout manually verified balanced and responsive at 1366x768. |
| 29 | **Maximized** | **MANUAL PASS** | POS layout manually verified maximized without visual distortion. |
| 30 | **Recall Dialog** | **MANUAL PASS** | Recall dialog search, filtering, and order preview panel verified usable with no critical clipping. |

---

## F. Technical Debt

- **DailySalesSequence Concurrency:** Per-day, per-warehouse sequential invoice numbers intentionally increment on completed sales. High-concurrency multi-terminal locking is documented active technical debt and preserved without regression.
- **Menu Item Image Store:** Photos stored as local files in `%LocalAppData%` rather than database binary blobs.
- **Cash/Card Substring Reporting:** End-of-Day report categorizes Cash vs. Card via substring checks on payment method name.
- **N+1 Entity Queries:** UI tile and summary renderers query prices and metadata in sequential loops.

---

## G. Next Restaurant POS Milestone

Based on the project roadmap (`01 Product Strategy/01.03 Product Roadmap.md`), the remaining unfinished Restaurant POS capabilities to address in subsequent milestones are:

1. **Kitchen Display System (KDS):** Digital kitchen bump bar screen and real-time line item preparation tracking.
2. **Advanced POS Reporting:** Detailed sales performance by category/hour, profit margins, and cashier performance breakdown.
3. **Direct Thermal Printer Spooling:** Hardware ESC/POS thermal printer communication for physical receipt and kitchen ticket printing.
4. **Multi-Terminal Concurrency Hardening:** Distributed locking for `DailySalesSequence` invoice generation.

---

## H. Product Acceptance Status

**Status: MANUAL USER ACCEPTANCE — PASS**  
The current Restaurant POS core order-entry, payment, hold/recall, and table workflows listed in this acceptance scope were manually tested through the running desktop application by the developer/user and confirmed working. Automated xUnit solution tests continue to pass (1100+ tests).

---

## I. Shift Management & Cash Register Balancing

### 1. Business Purpose & Scope
Shift Management establishes complete cash register drawer session lifecycle tracking, blind cash count reconciliation, cash movement (in/out) auditing, and shift history oversight without modifying or interrupting core Restaurant POS order entry.

### 2. Architecture & Domain Rules
- **Shift Aggregate Root (`Shift`):** Manages shift session state (`Open`, `Closed`), starting cash float, expected cash, counted cash, variance, and notes.
- **Single Active Shift Guard:** A terminal and a cashier may each have at most ONE open shift session at any given time.
- **POS Active Shift Check:** `RestaurantPosForm` ensures an active shift exists for the active terminal before allowing cash payment recording.
- **Cash Movement Entity (`CashMovement`):** Records drawer deposits (`CashIn`) and drops (`CashOut`) with user attribution, amount, and business reason.
- **Financial Calculation:**
  $$\text{Expected Cash} = \text{Starting Cash} + \text{CashIn} - \text{CashOut} + \text{Cash Sales}$$
- **Blind Cash Count:** At shift closure (`CloseShiftDialog`), cashiers enter actual counted cash without seeing system expected cash.
- **Variance Audit:** Monetary variance ($\text{Counted Cash} - \text{Expected Cash}$) requires an explicit business explanation if non-zero.
- **Shift History & Detail (`ShiftHistoryView`, `ShiftDetailDialog`):** Full audit reporting grid with date range and status filtering.

### 3. Implementation Status
**Status: IMPLEMENTED — PENDING MANUAL ACCEPTANCE**  
All domain logic, application commands/queries, EF Core persistence/migrations, authorization seeding, WinForms/DevExpress dialogs, POS integration, and unit test suites (128 domain tests + 152 application tests pass cleanly) have been implemented.



---

## J. POS PIN Login

### 1. Purpose
Cashiers sign in to the Restaurant POS with only a PIN — no username or password — while username/password login (and Back Office) continue to work unchanged.

### 2. Configuration Location
**Back Office → Administration → Users → (select user) → Set PIN / Clear PIN** (`UsersForm` toolbar, feature-gated by `feature.users.setpin`). The PIN entry dialog is `PinPromptForm` (New PIN + Confirm PIN, masked). The stored PIN is never displayed after saving; changing a PIN is always a fresh assignment, and Clear PIN removes the credential entirely (a user cannot be left with a guessable "empty" PIN).

### 3. Security / Storage
- PINs are hashed with the existing Authentication hashing infrastructure (`IPinHasher` / `Pbkdf2PinHasher`, PBKDF2 — same primitive as passwords). Only the hash is stored, in the existing `Authentication.UserCredentials.PinHash` column (nvarchar(512), nullable) — **no database schema change was required**.
- `PinPolicy.Default` enforces 4–6 digits, rejecting repeated ("1111") and sequential ("1234") PINs.
- PINs are **unique across users**: PIN-only sign-in resolves the user FROM the PIN, so a duplicate would make authentication ambiguous. Because PBKDF2 hashes are salted there is no indexable lookup value; uniqueness is enforced by `SetPinCommandHandler` verifying the candidate against every other user's stored PIN hash before assignment.
- Every PIN set/clear rotates the `SecurityStamp` (domain events `PinChanged`/`PinCleared`).

### 4. Authentication Flow
1. `LoginForm`: Username is no longer required when a PIN is entered.
2. `LoginService.LoginAsync` detects PIN-only input (blank username + PIN) and resolves the user by verifying the PIN against every stored PIN hash (`ResolveUserByPinAsync`).
3. The resolved user then passes through the SAME status checks (locked/inactive), audit records (`RecordLoginAttemptCommand`, `RecordCredentialCheckCommand`), session start (`StartSessionCommand`), and post-login module permission check (`menu.pos`) as a username/password login. Invalid PINs get the same generic "Invalid username or password." message — the UI never reveals whether a PIN is in use.

### 5. Session, Permissions & Shift Integration
PIN login establishes the identical `ICurrentSession` (UserId/SessionId/DisplayName) that username/password login would establish for the same user — there is no special "PIN user". Permissions are evaluated normally (module visibility, POS feature gates), and Shift Management therefore sees the correct cashier, because shift cashier identity is derived from the signed-in session user. PIN authentication does not bypass any permission, void, price-override, or payment authorization rule.

### 6. Automated Verification (application-level, NOT live UI)
`Clovent.Authentication.Application.Tests/Credentials/PinCommandTests.cs` (set/clear/policy/duplicate/no-plaintext) and `Clovent.Desktop.Tests/Login/LoginServiceTests.cs` (PIN-only resolution, unknown PIN, inactive user, multi-user disambiguation). These are automated integration tests — they do not by themselves prove the WinForms UI works.

### 7. Limitations / Technical Debt
- PIN resolution verifies every stored PIN hash (O(users) PBKDF2 verifications per PIN-only login) — acceptable at restaurant-scale user counts; a keyed HMAC lookup column is the scalability path if ever needed.
- Uniqueness is enforced in the application layer, not a database constraint (a consequence of salted hashing).
- `ManagerAuthorizationService` (feature-code re-auth prompts) still accepts password only.

---

## K. Table Occupancy Lifecycle & State Synchronization

### 1. Purpose
Defines the authoritative business rules and technical mechanisms governing real-time table occupancy status (`Available`, `Occupied`, `Reserved`, `OutOfService`) across order creation, cancellation, voiding, completion, hold/recall, application restart, and POS UI table selection.

### 2. Table Occupancy Business Rules
- **Active Seating:** A table is `Occupied` if and only if there is at least one active (`Open` or `Held`) Dine-In order assigned to that table.
- **Order Cancellation:** Cancelling a Dine-In order (`CancelOrderCommand`) releases table occupancy to `Available` if no other active (`Open` or `Held`) order is seated at that table.
- **Order Voiding:** Voiding a Dine-In order (`VoidOrderCommand`) releases table occupancy to `Available` if no other active order remains on that table.
- **Order Completion:** Completing a paid Dine-In order (`CompleteOrderCommand`) releases table occupancy to `Available` if no other active order remains.
- **Take Away Orders:** Take Away orders carry no table association (`TableId = null`) and never occupy any table.
- **Held Orders:** A Held Dine-In order represents a seated party and legitimately maintains table occupancy as `Occupied`.
- **Multi-Order Protection:** If multiple orders exist for a single table (e.g. historical/split data), completing or cancelling one order checks for remaining active orders on `TableId` before vacating the table, preserving `Occupied` state if another active order exists.

### 3. UI Synchronization & Persistence
- After cancelling or voiding an order in `RestaurantPosForm`, `await ReloadTablesAsync()` is invoked to fetch updated table statuses from the database via `ListAllTablesQuery`.
- `TablePickerEdit` (`_tablePicker`) updates its internal items list and display strings immediately so table availability (`T-03 (Available)`) is reflected without needing application restart.
- Occupancy changes are transactionally committed to the database per MediatR handler unit of work (`UnitOfWorkBehavior`).

### 4. Verification
- **Automated Tests:** Covered in `Clovent.Restaurant.Application.Tests/Orders/OrderHandlerTests.cs` (`CancelOrderCommandHandler_DineIn_VacatesTable`, `CancelOrderCommandHandler_DineIn_KeepsTableOccupied_WhenAnotherActiveOrderExists`, `VoidOrderCommandHandler_DineIn_KeepsTableOccupied_WhenAnotherActiveOrderExists`, `TakeAwayOrder_DoesNotOccupyTable`, `ReopenOrderCommandHandler_DineIn_ReoccupiesTable`).

---

## L. POS Menu Item Eligibility & Category Count Reconciliation

### 1. Purpose
Defines the canonical eligibility rules for displaying menu items in the POS tile grid and calculating tab counts for **All Menu** and individual category tabs.

### 2. POS Menu Item Eligibility Rules
A menu item is eligible for display in the POS if and only if:
- The `ProductVariant.Status` equals `"Active"`.
- The parent `Product.Status` equals `"Active"`.
- The `Product` is not deleted/soft-deleted.

### 3. Category Count & Reconciliation Rules
- **All Menu Count:** Calculated as `COUNT(DISTINCT ProductId)` across all eligible active variants.
- **Category Tab Counts:** Calculated as `COUNT(DISTINCT ProductId)` of eligible active variants belonging to each active category.
- **Uncategorized Handling:** Any eligible active variant where `ProductCategoryId` is `null` or points to an inactive/deleted category is rendered under an explicit **Uncategorized** category tab (`🏷️ Uncategorized`).
- **Mathematical Formula:**
  $$\text{All Menu Count} = \sum_{c \in \text{Active Categories}} \text{Count}(c) + \text{Count}(\text{Uncategorized})$$
- This rule guarantees that every single item listed in **All Menu** is fully accounted for by the visible category tabs without any discrepancy.

---

## M. POS Menu Item Category Assignments

### 1. Purpose
Defines standard category assignments for Pakistani cuisine menu items in the CBOS Restaurant POS catalog to ensure zero uncategorized items while preserving domain entities, variants, prices, barcodes, and images.

### 2. Category Assignment Matrix
| Product Name | SKU | Target Category | Portions / Variants | Standard Price(s) |
|---|---|---|---|---|
| **Chicken Karahi** | `CHICKEN-KARAHI` | `Karahi` | Standard | 1200 |
| **Chicken Koyla Karahi** | `CHICKEN-KOYLA-KARAHI` | `Karahi` | Half Plate, Full Plate | 350 / 550 |
| **White Daal Mash** | `WHITE-DAAL-MASH` | `Main Course` | Half Plate, Full Plate | 220 / 340 |
| **Aloo Chicken Qorma** | `ALOO-CHICKEN-QORMA` | `Main Course` | Standard | 650 |
| **Chicken Biryani** | `CHICKEN-BIRYANI` | `Main Course` | Standard | 450 |
| **Chicken Haleem** | `CHICKEN-HALEEM` | `Main Course` | Half Plate, Full Plate | 260 / 420 |
| **Murgh Chanay** | `MURGH-CHANAY` | `Main Course` | Half Plate, Full Plate | 260 / 400 |
| **Aloo Gobi** | `ALOO-GOBI` | `Main Course` | Half Plate, Full Plate | 250 / 380 |
| **Salad** | `SALAD` | `Salads` | Standard | 30 |

### 3. POS Category Totals & Reconciliation (Live Database `Clovent_Catalog`)
- **Beverages**: 1 item (`Leechi`)
- **Bread**: 1 item (`Garlic Nan`)
- **Karahi**: 2 items (`Chicken Karahi`, `Chicken Koyla Karahi`)
- **Main Course**: 6 items (`White Daal Mash`, `Aloo Chicken Qorma`, `Chicken Biryani`, `Chicken Haleem`, `Murgh Chanay`, `Aloo Gobi`)
- **Salads**: 1 item (`Salad`)
- **Chicken**: 0 items
- **Snacks**: 0 items
- **Uncategorized**: 0 items
- **Total All Menu**: `1 + 1 + 2 + 6 + 1 = 11 items`

### 4. Category Seed Startup Enforcement
Category enforcement is managed via `EnsurePakistaniCuisineCategoryAssignmentsAsync` in `DevelopmentCatalogSeedStartupTask.cs`. This task executes unconditionally on every application startup prior to early-return guards, guaranteeing that canonical category assignments (`Karahi` = 2, `Main Course` = 6, `Salads` = 1, `Uncategorized` = 0) are maintained idempotently across restarts.

---

## N. POS Default Customer Architecture & Acceptance

### 1. Purpose & Core Invariants
- **Real Persisted Entity:** The default customer is a real, editable, and renameable record in `[Restaurant].[Customers]` (seeded as `C000`, `Walk-in Customer`, `IsDefault = 1`, `IsActive = 1`), eliminating hardcoded `Guid.Empty` or null pseudo-customers.
- **Single Active Default Customer:** Exactly one customer across the entire restaurant system may have `IsDefault = true`. Assigning a new default customer unsets the previous default in the same transaction.
- **Active Status Constraint:** Only an active customer (`IsActive == true`) can be default. Deactivating the default customer automatically clears its default status.
- **POS Order Entry Binding:** Creating a new POS order automatically assigns and selects the active default customer record. Cashiers can change the customer anytime. Changing the default customer never retroactively modifies historical or open orders.
- **Customer Lookup Protection:** Internal GUIDs (`CustomerId`) remain strictly hidden from POS cashiers; the lookup popup displays only `Customer Code`, `Name`, `Phone`, and `Balance`.

### 2. Manual Acceptance Checklist for Solution Architect

| Step | Verification Scope | Expected Behavior |
|---|---|---|
| **1** | **Customer Management Configuration** | In Back Office → Customers: verify `Default` column shows `YES` for `C000 Walk-in Customer`. Check that `Set as Default` button enables for active non-default customers and unsets the previous default upon selection. Verify the `Default Customer` checkbox in the edit form works. |
| **2** | **New Dine-In / Take Away Orders** | In Restaurant POS: select a table (Dine-In starts automatically) or click `+ Take Away`. Confirm the customer dropdown automatically defaults to the designated default customer (`C000 - Walk-in Customer`) without cashier interaction. |
| **3** | **Customer Selector Visibility** | Click the customer dropdown in POS. Confirm popup headers and visible columns show only `Customer Code`, `Name`, `Phone`, and `Balance`. Verify internal database GUID (`CustomerId`) is NOT visible. |
| **4** | **Hold and Recall Order** | Create an order with the default customer (or select a different customer), add items, and click `Hold`. Open `Recall Order`, verify the customer code and name appear in the recall dialog list and preview, and click `Recall`. Confirm 100% data fidelity is restored without regression. |
| **5** | **Customer Rename & Persistence** | In Customers management: rename `Walk-in Customer` to `Counter Guest`. Return to POS, start a new order, and verify the selector immediately displays `Counter Guest` without creating duplicate records or resetting to hardcoded strings. |

### 3. Acceptance Status
- **Status:** **MANUAL ACCEPTANCE — PASS**
- **Verified By:** Solution Architect
- **Verification Date:** 2026-09-14
- **Verification Summary:** All 5 manual verification steps (Customer Management Default configuration, automatic assignment on new orders, customer popup GUID concealment, Hold/Recall preservation, and customer renaming) tested and confirmed working against the live desktop application.

---

## N-2. Table-Selection Auto Dine-In — Change Record (2026-09-15)

### 1. Behavior Change
The "+ Dine In" header button was **removed**. Selecting a valid table in the Table dropdown is now the sole and sufficient trigger for Dine-In: with no working order, the POS immediately creates the Dine-In working order seated at that table (reusing the existing `CreateOrderCommand` path, including default-customer assignment), refreshes the header (`ORD-XXX • Dine In • Open`, `Table: T-xx`) and Active Orders, and item entry is ready with no further clicks. `+ Take Away` is unchanged and remains table-free.

### 2. Rules & Edge Cases
- "(No Table)" never creates an order.
- A table seating an Open/Held order surfaces that order (auto-resume if Held) rather than creating a duplicate; the pre-existing hold-before-switch prompt and `TransferOrderTableCommand` validations (Reserved/OutOfService refusal, occupied-table rejection) apply unchanged.
- Changing tables before adding items transfers the single working order — no duplicate orders.
- An existing working order is never duplicated by a picker change (`TableSelectionDineInPolicy`).
- If no warehouse is selected when a table is picked, the existing "Select a location first" warning is shown and no order is created.
- Take Away disables the table picker, so a Take Away order cannot accidentally acquire a table.
- Hold/Recall, Clear, Active Orders, payment/completion, Quick Orders and category selection are unaffected; completed orders persist `OrderType = DineIn` and `TableId`.

### 3. Automated Test Coverage
- `Clovent.Restaurant.Application.Tests/Orders/TableSelectionDineInTests.cs` (7 tests): DineIn+TableId on creation, TakeAway table-free, immediate item entry after creation, Hold/Recall preservation, completion preservation, table change before items creates no duplicates, occupied-table second-order rejection.
- `Clovent.Desktop.Tests/Restaurant/Orders/TableSelectionDineInPolicyTests.cs` (3 tests): UI decision policy (valid table auto-starts, no-table does not, existing order never duplicates).
- Full suites re-run: Restaurant.Application.Tests 245/245 pass; Desktop.Tests (excluding live suites) 327/327 pass.

### 4. Live Verification
- `LiveAcceptanceQaRun` extended with a Phase 23 that drives the **real Restaurant POS form against the live database**: it picks a free table in the actual table dropdown (raising the real `SelectionChanged` event, no Dine-In click), asserts the working order becomes Dine-In at that table, adds an item through the form's own add-item path on the UI thread, and asserts the line persisted. Screenshot: `qa/live_27_auto_dinein_table.png` (verified visually: header shows `ORD-303 • DineIn • Open`, `Table: T-QA842`, 1 item, no "+ Dine In" button).
- Full live acceptance suite (all phases incl. Hold/Recall, payment, Place Order, Active Orders, resolutions): **PASS** (2026-09-15).
- Additional cashier-level manual pass over the running desktop app is planned/being performed by the operator.

### 5. Known Technical Debt
- ~~The QA live suite may leave behind `T-QA###` tables and a held QA order~~ **Resolved 2026-09-16:** the live suite now voids every order it creates at the end of each run and reuses a single `T-QA` table (creating one only when the floor is full), leaving the POS floor exactly as it found it. A one-off manual cleanup on 2026-09-16 also removed the accumulated QA backlog (105 QA orders, 4 `T-QA###` tables, the `QACUST01` QA customer); legitimate customers `C000`–`C004`, tables `T-01`–`T-03` and all genuine manual orders were preserved. The suite's control order `ORD-98` is intentionally retained and reused across runs.
- `CreateOrderCommandHandler`'s open-order-per-table check remains check-then-act (documented in the handler; needs a DB filtered unique index for a hard guarantee).
- This change alone does not make the whole Restaurant POS sell-ready; overall readiness is tracked by the existing acceptance tables in this document.

---

## O. CBOS Smart Restaurant POS Architecture & Capabilities

### 1. Smart Upsell Engine
- **Purpose:** Contextual, deterministic product recommendations when cashier builds an order.
- **Rule Architecture:** Configured via `RecommendationRule` entity (`[Restaurant].[RecommendationRules]`), supporting:
  - `ProductId` (trigger item) or `null` ("Any Basket" trigger)
  - `RecommendedVariantId` (upsell target)
  - `Priority` (ordering)
  - `IsActive` (toggle status)
  - `StartTime` / `EndTime` (time window restriction)
  - `DaysOfWeek` (bitmask day restriction)
  - `Notes` (operator documentation)
- **UI Integration:** Compact DevExpress `PopupContainerEdit` trigger strip docked at the Top of the right-side cart panel (`_pnlOrderedItemsContainer`), directly above the ordered items list. Replaces both the obsolete separate modal dialog popup and the rejected permanently-expanded embedded grid with a professional temporary dropdown overlay pattern.
- **Compact Trigger & Dropdown Overlay Suggested Add-ons (2026-09-18):** Back Office Recommendation Rule -> `GetBasketRecommendationsQuery` -> compact trigger strip above cart (`[bulb icon] Suggested Add-ons [ N suggestions ▼ ]`) -> click opens floating `PopupContainerControl` dropdown overlay -> multi-select checkboxes `[✓]` -> `[Add Selected (N)]` -> normal canonical cart pipeline -> `SuggestionEvents` -> Upsell Performance.
  - **Compact Trigger Strip:**
    - Zero recommendations: completely hidden (`Height = 0`, `Visible = false`), taking 0px of cart space. The cart starts at the very top of `_pnlOrderedItemsContainer`.
    - 1+ recommendations: consumes only a single compact control row (~28px). Displays DevExpress SVG bulb icon (`actions_idea`), "Suggested Add-ons" label, and `PopupContainerEdit` combo editor displaying `[ N suggestions ▼ ]`.
    - Clicking the bulb icon, label, or dropdown editor opens the temporary dropdown overlay.
  - **Temporary Dropdown Overlay (`PopupContainerControl`):**
    - Sized dynamically to fit items without wasted space (~400px width, dynamic height scaled for 1–4 items, capped at 4 items with vertical scrolling for 5+).
    - Floats over the POS content temporarily; does NOT permanently alter or shift the cart layout.
    - Closes cleanly when clicking outside, pressing Escape, or completing item additions.
  - **Checked Grid:** Multi-select DevExpress `GridControl`/`GridView` with checkboxes (`[✓] | Item | Portion | Price`). Item names are displayed in full width without premature ellipsis truncation. Portions matching standard/regular display cleanly as `-`. Touch/click anywhere on a row or pressing Space toggles selection.
  - **Footer Controls:** A `[☐ Select All]` checkbox (toggles all rows, automatically tracks partial/full selection) and an `[Add Selected (N)]` button (disabled when N=0; updates label dynamically as rows are checked/unchecked; styled with POS teal accent color).
  - **Cart Layout & Pipeline Integrity:** When the dropdown is closed, the cart occupies essentially the exact same vertical space as before recommendations. Adding selected items reuses `AddProductToCurrentOrderCoreAsync` with proper merging, order mutation locking, and analytics tracking (`SuggestionEventKind.Accepted` with actual item price and order line id; `Offered` tracked once per variant per order). Accepted items are immediately removed from the suggestions on basket refresh.
- **Availability Filtering (2026-09-17):** `GetBasketRecommendationsQueryHandler` now excludes any recommended product or variant whose `CatalogStatus` is not `Active` - inactive catalog items can never be suggested, for configured rules and "popular today" fallback alike.
- **Suggestion Analytics (2026-09-17):** Each suggestion interaction writes an immutable `SuggestionEvent` row (`[Restaurant].[SuggestionEvents]`): `Offered` (once per variant per order, so repeated strip refreshes never inflate counts), `Accepted` (only when the cashier adds the item through the suggestion grid; snapshots the added line's quantity and unit amount so upsell revenue is attributed exactly to the interaction), and `Dismissed`. Writes are fire-and-forget and never block checkout; normal menu additions never produce events, so they are never counted as accepted upsells.
- **Upsell Performance Reporting (2026-09-17):** Back Office → Restaurant → Smart POS → **Upsell Performance** aggregates the events over a selectable date range: Recommended Item, Variant, Offers, Accepted, Dismissed, Conversion %, and Upsell Revenue (sum of accepted quantity x unit amount). Product/variant names resolve live from the catalog; nothing is stored redundantly on the events.
- **Deal Preview Label (2026-09-17):** `QuickOrderPreviewDialog` header price now reads `Deal Price:` (the template's configured `TotalPrice`), alongside the existing bottom `Deal Total:` row.
- **Development Upsell Verification Configuration (2026-09-17):** Active development rules in `[Restaurant].[RecommendationRules]` configured for end-to-end POS upsell verification:
  - **Rule ID:** `214EFDD3-9149-446C-815E-8DA6FEBBA9F5`
  - **Trigger Product:** `Chicken Biryani` (Product ID: `7E23EAAD-1BE3-427B-8E0E-0EF152AA49A7`)
  - **Recommended Product 1:** `Salad` (Variant ID: `A6EF7092-DAB1-4E73-A603-27001BD6D85D`, Price: Rs. 30.00, Priority: 1)
  - **Recommended Product 2:** `Leechi` (Variant ID: `62B73E75-129E-4489-9AD8-6C8C96FCB527`, Price: Rs. 50.00, Priority: 2)
  - **Runtime POS Workflow:** When `Chicken Biryani - Standard` is added to a Dine-In or Takeaway cart, the compact trigger strip appears directly above the cart with `[bulb icon] Suggested Add-ons   [ 4 suggestions ▼ ]`. The cart rows remain visible immediately below. Clicking the trigger opens the dropdown overlay grid showing `Salad`, `Leechi`, `Chicken Haleem - Half`, and `Aloo Gobi - Half`. Selecting 3 items updates the button to `[Add Selected (3)]`. Clicking it commits all 3 items to the cart, closes the overlay, recalculates totals, records 3 `Accepted` suggestion events, and updates remaining suggestions.
- **Remaining smart features (not yet implemented):** Frequently-bought-together mining from order history, order-value thresholds ("add Rs. X more"), customer-history-based recommendations, margin-aware prioritization, and low-stock-aware suppression. The `SuggestionEvent` analytics model and the rule/priority engine are the architectural foundation these would build on.
- **Manual verification pending:** Compact trigger and DevExpress dropdown overlay Suggested Add-ons interaction await user manual acceptance testing.

### 2. Quick Order Templates
- **Purpose:** Single-click order bundles for high-volume combos (e.g. Family Deals, Combos).
- **UX Hierarchy & Placement:**
  ```
  FOODIES MENU HEADER
  ↓
  CATEGORY BAR
  ↓
  QUICK ORDERS COLLAPSIBLE STRIP (Row 2 of center TableLayoutPanel, directly below categories)
  ↓
  PRODUCT VIEWPORT / GRID
  ↓
  PAGINATION FOOTER
  ```
- **Strip Layout & State:**
  - **Collapsed State:** Shows compact toggle button `⚡ Quick Orders ▸` with row height ~28px.
  - **Expanded State:** Shows `⚡ Quick Orders ▾` and horizontal flow panel with active deal buttons (e.g. `[Family Biryani Deal · Rs. 2,060]`, `[Chicken Karahi Feast · Rs. 1,560]`) with row height ~44px.
  - **Click Behavior — Deal Preview (2026-09-16):** Cashier clicking `⚡ Quick Orders ▸` toggles smoothly without page jumping or dock collisions. Clicking a deal now opens a lightweight preview dialog (`QuickOrderPreviewDialog`) showing the deal's REAL database-configured contents — deal name, total, description, and per-item quantity × product (variant) with resolved unit and line prices (the same `ListActiveQuickOrderTemplatesQuery` expansion the strip uses: `TemplateUnitPrice` override wins, otherwise the variant's current catalog selling price). Nothing is added on the first click. **Add Deal** (or Enter → OK) replays the items through the ordinary `AddProductToCurrentOrder` pipeline into the working cart, preserving quantities, variants and all existing pricing/discount/tax/service-charge logic and the current order context (no second order is created); **Close**/Esc aborts with zero effect on the active order. The deal model currently has no configurable-choices concept — every template item is fixed; if a choices feature is added to `QuickOrderTemplateItem` later, the preview must grow the selection UI. Preview contents refresh automatically because the strip reloads templates on every `ReloadQuickOrderTemplatesAsync` (menu change, order refresh).
- **Template Architecture:** Configured via `QuickOrderTemplate` and `QuickOrderTemplateItem` entities (`[Restaurant].[QuickOrderTemplates]`, `[Restaurant].[QuickOrderTemplateItems]`), supporting:
  - Name, Description, DisplayOrder, IsActive
  - Items with `ProductVariantId`, `Quantity`, optional `ItemPrice`
- **POS Behavior:** Expands items through ordinary `AddProductToCurrentOrder` command pipeline, respecting all variant and duplication rules.

### 3. Order Health / Waiting Time
- **Purpose:** Real-time visual aging indicators for active/held orders without UI flicker.
- **Threshold Service:** Evaluates elapsed time from order creation:
  - 🟢 Normal / Ready: 0–10 minutes
  - 🟠 Waiting: 10–20 minutes
  - 🔴 Warning / Delayed: 20+ minutes
- **Performance:** Lightweight 15-second timer updates only targeted card status text without rebuilding controls.

### 4. Rush Mode (⚡)
- **Purpose:** Optimizes presentation for peak rush periods.
- **Behavior:** Disables non-critical animations, suppresses auto-popup suggestion strips, maintains immediate access to key controls.
- **State:** Persisted in `PosSettingsStore` and toggled via More menu.

### 5. Smart Universal Search
- **Purpose:** Unified search across products, customer codes, names, phones, order numbers, and tables.
- **UI:** Compact `UniversalSearchDropdown` with category headers (`🍽 Products`, `👤 Customers`, `🧾 Orders`, `🪑 Tables`), debounce, and keyboard navigation (Up/Down/Enter/Esc).

### 6. One-Click Customer Reorder
- **Purpose:** Instant replay of a customer's last order or frequent items.
- **Validation:** Verifies active catalog status of every line; skips inactive/discontinued items with clear cashier notification.

### 7. Restaurant Pulse
- **Purpose:** Instant operational dashboard overlay.
- **Metrics:** Sales total, order count, average order value, best-seller, top revenue item, sales vs yesterday, low-stock alerts, average order fulfillment time, and beverage upsell opportunity.

### 8. Back Office Configuration Screens
- `QuickOrderTemplatesView` / `QuickOrderTemplateEditForm`: Template creation and item management. Sizable Back Office dialog (~860x620 logical pixels, MinimumSize 760x520) featuring complete Product → Variant → Auto-fetched Selling Price selection workflow, manual unit price overrides for deal bundling, existing template binding with saved deal price preservation, and line total live calculation.
- `RecommendationRulesView` / `RecommendationRuleEditForm`: Rule priority, time-window, and day filters.
- `OrderHealthSettingsForm`: Configurable warning and danger thresholds.

### 9. Permissions
- `pos.smartinsights`: Smart recommendations and customer insights.
- `pos.quickorders`: Quick order templates.
- `pos.restaurantpulse`: Restaurant Pulse dashboard.
- `pos.rushmode`: Rush Mode presentation toggle.
- `quickordertemplates.*` / `recommendationrules.*`: CRUD and activation.
- `upsellperformance.view` (2026-09-17): Back Office Upsell Performance analytics grid (Restaurant → Smart POS → Upsell Performance).

## Smart Combo Builder & Quick Order Deals (2026-09-21)

Restaurant → Smart POS → Smart Combo Builder mines completed, warehouse-scoped sales for variant pairs/triples and explains frequency, support, directed attach rate and lift. Managers preview prices and available catalog-cost margin estimates, edit a name/price, then explicitly Create Deal into existing QuickOrderTemplates. The established POS Quick Orders → Deal Preview → Add Deal flow is reused. Dismissals persist for a configurable period; no deals are automatically published.

### 250% High-DPI & Professional UI Verification Pass:
1. **DPI Architecture:** Preserved the established `DesktopDpi.Scale(...)` architecture and fixed 250% scaling (DeviceDpi 240) baseline across all controls, fonts, row heights, and dialog dimensions.
2. **Main Application Chrome:** Removed temporary `CBOS DEBUG | Build ...` title text and replaced with clean `Clovent Business Operating System`. Added multi-resolution brand icon (`cbos.ico`) configured as `<ApplicationIcon>` in the csproj and assigned to `MainForm.Icon` for taskbar and window shell rendering.
3. **Smart Combo Main Screen:** Manually verified improved at 250% DPI. Polished `Suggested Price` column header width and DPI scaling to prevent truncation when DevExpress column filter glyphs appear. Removed temporary debug watermark `LAYOUT BUILD SCB-20260921-B`.
4. **Smart Combo Opportunity Popup Rebuild:** Completely redesigned presentation from compressed utility popup into a professional DPI-aware sizable dialog (~760x650 scaled, min 680x580):
   - **Suggested Combo:** Full-width name editor with clean label hierarchy.
   - **Structured Items Grid:** Compact read-only DevExpress `GridControl`/`GridView` with `ColumnAutoWidth = true` replacing loose plain-text formatting, with right-aligned `Normal Total` summary.
   - **Sales Evidence:** Multi-column structured KPI cards (Bought Together, Eligible Orders, Support, Attach Rate, Lift, Period) with explainable direction notes.
   - **Deal Settings & Margin:** Clear separation between Suggested Deal Price, live Discount %, and Estimated Margin, featuring a dedicated informational/warning banner for unverified cost data.
   - **Full-Width Dismiss Reason:** Unclipped dropdown with standard domain reason selections.
   - **Action Footer:** Right-aligned `[ Close ] [ Dismiss ] [ Create Deal ]` buttons with proper DPI scaling and Esc shortcut handling.
5. **Quick Order Items Grid Correction:** Resolved the 1/3-width squashed grid issue by enabling `_itemsView.OptionsView.ColumnAutoWidth = true`, assigning intentional column weight proportions (Product 28%, Variant 22%, Qty 10%, Deal Price 13%, Catalog Price 13%, Line Total 14%), DPI-scaling row heights, and eliminating header truncation (`Varia...`, `De...`, `Ca...`, `Line...`). Removed `LAYOUT BUILD QOT-20260921-B` watermark.
6. **Quick Order Modal Lifecycle Fix (2026-09-22):** Resolved the critical bug where double-clicking a template row caused the edit dialog to reopen after closing via the top-right Windows 'X' button or Cancel. Removed duplicate `_gridView.RowCellClick` event handler, centralized edit opening into `OpenSelectedTemplateForEditAsync()`, enforced hit-testing in `_gridView.DoubleClick` (`CalcHitInfo`), and implemented an `_isEditDialogOpen` reentrancy guard. Formatted discrete quantities (`0.##`) so items display whole integers (`1`, `2`, `4`) rather than floating precision (`1.000`, `2.000`, `4.000`).
7. **Restaurant Setup Screen Redesign & Unified Save (2026-09-22):** Redesigned Restaurant Setup from a fragmented 2-column layout into a vertically stacked enterprise settings view with consistent max width (`840px` scaled), uniform field label alignment (`200px`), and fixed POS layout settings (2-column Show/Hide radio group with zero clipping). Replaced the three separate save actions and oversized floppy disk icons with a single primary `[ Save Settings ]` button persisting Order Numbering, Display Language, and POS layout settings atomically.
8. **Restaurant Setup POS Layout Rebuild & High-DPI Scaling (2026-09-22):** Rebuilt the POS Layout card to match the visual fidelity of Order Numbering. Applied generous vertical row spacing (`Padding(0, 6, 0, 6)`), widened the Show/Hide radio group to `220px` with 2 distinct columns, added distinct separation above helper text (`Padding(0, 12, 0, 6)`), and implemented runtime card minimum-height calculation (`EnsureCardHeight`) preventing text and control clipping across 100% to 250% DPI scaling.
9. **Authorization Architecture & Idempotent Database Upgrade (2026-09-22):** Documented CBOS authorization architecture (strict Model B RBAC; no superuser bypass or debug overrides). Resolved canonical permission code `feature.restaurantsetup.edit` (with navigation permission `menu.restaurantsetup`). Refactored startup task `DevelopmentAuthorizationSeedStartupTask` to ensure existing SQLite databases are idempotently upgraded with all system permissions and `Administrator` mappings without requiring manual SQL execution or database recreation.
10. **Shift History Responsive Rebuild & Report Periods (2026-09-22):** Replaced legacy fixed 50px/45px row styles with a responsive 3-row `TableLayoutPanel` (`AutoSize` top, `Percent 100f` grid, `AutoSize` bottom action bar). Added top filter bar with 11 QuickBooks-style period presets (Today, Yesterday, This Week, Last Week, This Month, Last Month, This Quarter, Last Quarter, This Year, Last Year, Custom), two-way From/To date synchronization, Status filter, Search, and Clear Filters action. Added custom empty-state overlay (`"No shifts found for the selected period."`). Fixed bottom action bar button sizes with `DesktopDpi.Scale` and updated caption to `"Cash In / Cash Out"`.
11. **Sales Summary Shared Report Period Integration (2026-09-22):** Traced ribbon control `Sales Summary` to `EndOfDayReportView`. Implemented reusable `ReportPeriod` enum and `ReportPeriodCalculator` with strict business-local date arithmetic and UTC query range semantics (`range.StartOfFromUtc` to `range.EndOfToUtcInclusive`). Integrated into both Sales Summary and Shift History views.
12. **Restaurant Setup POS Layout Polish & Sales Summary Cleanup (2026-09-22):**
    - **Active Orders Row Polish:** Replaced the tall white RadioGroup box with a borderless, transparent-background control (`BackColor = Color.Transparent`, `Appearance.BackColor = Color.Transparent`, `AppearanceFocused.BackColor = Color.Transparent`, `Padding = 0`) at a compact height of `26px` scaled. Configured 2 horizontal columns for `Show` and `Hide` options. This achieves identical vertical cadence to `Items Per Row` and `Default Payment Method` without any clipping or white card surfaces.
    - **Restaurant Setup Viewport & Scrollbars:** Reduced header panel height to `54px` and tightened scroll container and card margins (`(0, 10, 0, 10)`), allowing the full settings view to fit within the viewport without triggering vertical scrollbars when maximized on 1080p screens at standard scaling.
    - **Sales Summary Filter Bar Streamlining:** Removed redundant `[ Today ]` and `[ Yesterday ]` shortcut buttons from `EndOfDayReportView`. The `Report Period` dropdown preset list (Today, Yesterday, This Week, Last Week, This Month, Last Month, This Quarter, Last Quarter, This Year, Last Year, Custom) is now the single source of truth for date range presets, keeping the toolbar clean on one horizontal row: `Location -> Report Period -> From -> To -> [ Generate ] [ Print Summary ]`.
    - **Currency & KPI Card Polish:** Verified that `EndOfDayReportView` strictly relies on canonical `CurrencyDisplay.Format(decimal)` and `CurrencyDisplay.FormatPlain(decimal)` powered by `MasterData` settings (no hardcoded "Rs." strings). Compacted secondary statistics strip (`VOIDED ORDERS` and `AVERAGE SALE`) with Segoe UI typography and clean `32px` separation.
13. **Customer Ledger, Sales Summary & Upsell Quality Pass (2026-09-22):**
    - **Customer Ledger Functional & High-DPI Fix:** Restructured filter controls into a 2-row layout (`RowStyles` `AutoSize`) with dedicated `Report Period` preset dropdown (Today, Yesterday, This Week, Last Week, This Month, Last Month, This Quarter, Last Quarter, This Year, Last Year, Last 7 Days, Last 30 Days, Custom) defaulting to `This Month`. Synchronized From/To date inputs. Enforced `range.EndOfToUtcInclusive` for queries. Added active loading feedback (`_btnLoadLedger.Enabled = false`, cursor `WaitCursor`, `_lblStatus` reporting transaction count). Aligned action buttons (`[ Load Ledger ]`, `[ Clear ]`, `[ Print ]`, `[ PDF ]`, `[ Excel ]`) on a single horizontal baseline with unified height. Documented Walk-in Guest (`C000`) semantics (walk-in retail settlements bypass accounts receivable credit ledger, rendering 0.00 balances legitimately).
    - **Sales Summary Main Action Bar Consolidation:** Moved `Preview`, `Print`, `Export PDF`, and `Export Excel` out of individual detail tab pages into the primary filter and action toolbar immediately preceding `[ Print Summary ]`. Removed redundant tab-local toolbars from `Top Selling Items`, `Cash Summary`, `Bills`, `Inventory Movement`, and `Stock Remaining`. All reporting actions dynamically target the currently active tab. Clarified distinction between `Print` (DevExpress print preview for the active grid) and `Print Summary` (full session receipt statement dialog).
    - **Top Selling Items Product-Variant Enrichment:** Resolved bare variant name rendering ("Standard", "Half Plate") by querying `ListProductsQuery()` alongside variants and joining parent product titles to render full business names (e.g. `Chicken Biryani - Standard`, `Chicken Haleem - Half Plate`).
    - **Upsell Performance Enterprise Standardization:** Replaced bare utility toolbar with standard enterprise page header (`UPSELL PERFORMANCE` + subtitle) and canonical `Report Period` selector (defaulting to `Last 30 Days`). Added empty-state handling and unified `CurrencyDisplay.FormatPlain` formatting across revenue metrics.
    - **Restaurant Setup Active Orders Micro-Alignment:** Lowered `Active Orders` label and radio controls by 4 logical pixels to achieve baseline harmony with adjacent text and inputs.
14. **Customer Ledger & Back Office Final Polish (2026-09-22):**
    - **Dedicated Status Strip & Layout Reorganization:** Fixed status label overlap by moving `_lblStatus` out of `filterPanel` into its own dedicated row (Row 3 of `root`, `_statusPanel`), docked directly between filters and `_ledgerGrid`. Restructured `filterPanel` with a full-width search input in Row 3 and an unclipped tools bar in Row 4 featuring left-docked `[ Load Ledger ]` (Teal primary) and `[ Clear ]`, and right-docked `[ Print ]`, `[ PDF ]`, `[ Excel ]` export buttons.
    - **Mathematical Running Balance & Opening Balance Brought Forward Reconciliation:** Resolved the root cause where clicking Load Ledger produced 0 rows for customers with balances. Customers whose credit transactions occurred in previous months (such as John Smith, Jazib, Rafiq in August 2026) now calculate an Opening Balance Brought Forward (`priorEntries.Sum(Debit) - priorEntries.Sum(Credit)`) when a date range starting after the initial transactions is selected (such as `This Month` = September). In addition, added `"All Time"` preset and wired `Clear` to reset to `"All Time"`, allowing operators to view all transactions across all time without date cutoff.
    - **Walk-in Guest (`C000`) Ledger Semantics:** Verified Walk-in Guest (`C000`) counter sales settle immediately upon payment and do not create accounts receivable credit records. Added customized empty-state overlay explaining this domain rule (`"Walk-in guest has no credit ledger transactions. Counter sales are settled immediately upon payment."`).
    - **Customers Grid Column Cleanup:** Cleaned up redundant and confusing phone columns in `CustomersView`. Retained `Mobile` (`MobileNumber`) and `Phone` (`Phone`), while moving `Alt. Mobile` (`Mobile2`) and `Shop No` (`ShopNo`) to hidden grid columns available in the Column Chooser. Added scaled minimum widths preventing column title truncation.
    - **Reentrancy Protection:** Added `_isLedgerDialogOpen` guard in `CustomersView.cs` and `_isLoading` guard in `CustomerLedgerDialog.cs` to prevent multi-dialog spawning and duplicate simultaneous backend query execution.
16. **Restaurant POS Operations DropDown & Single-Window Navigation (2026-09-23):**
    - **Header Operations Command Menu (`Operations ▾`):** Integrated an extensible DevExpress `DropDownButton` + `DXPopupMenu` command menu in the POS header (`tlpHeaderNew`, column 8) adjacent to Logout. Styled with Segoe UI bold typography, consistent `44px` scaled minimum height, auto-sizing, and dropdown arrow.
    - **Permission-Gated Back Office Command:** Provides a direct bridge from POS back to the Back Office shell. The button and menu item dynamically adapt to user permissions (`menu.dashboard` or admin role); users lacking back-office access cannot navigate out of POS.
    - **Extension Points Architecture:** Initialized the architectural slot for `Refund / Return` with permission gating and a visual separator (`BeginGroup = true`). Because CBOS currently provides payment voiding on open/history orders but lacks a completed-order merchandise return/refund domain aggregate, the command is kept hidden until a safe domain workflow is implemented. Cleanly accommodates future POS operations (`Customer Balance`, `Customer History`, `Reprint Receipt`, `Price Check`, `Cash Movement`, and `Shift Information`).
    - **Single-Window Application Lifetime Invariant:** Enforced exactly ONE top-level form active at runtime. Navigating between POS and Back Office cleanly closes and disposes the prior window; no hidden, minimized, or background forms persist in memory (`Application.OpenForms`). Application process lifecycle is managed by `CbosApplicationContext` (`System.Windows.Forms.ApplicationContext`), ensuring window transitions do not terminate the process while closing the active window via its 'X' button cleanly exits the application.
    - **Transient Cart Navigation Guard:** Navigating from POS to Back Office inspects working cart state via `HasInProgressOrder()`. Empty carts or held orders navigate immediately. In-progress working carts with items present `PosNavigationGuardDialog`, offering:
      1. `[ Hold & Open Back Office ]` (`DialogResult.Yes`): Atomically holds the working order (`HoldOrderCommand`), refreshes local state, and completes navigation to Back Office.
      2. `[ Stay in POS ]` (`DialogResult.Cancel`): Aborts navigation, keeping the cashier in POS with the active cart completely intact.
    - **Session & Shift State Preservation:** Cashier shifts, held orders, active order states, and authenticated user credentials survive navigation transitions transparently.
    - **Reentrancy Protection:** All transitions are secured by an `_isTransitioning` atomic guard on `IApplicationModeNavigator` to eliminate double-activation or orphaned window instantiation under rapid clicking.

18. **Restaurant POS Operations Menu Control Pattern Alignment (2026-09-23):**
    - **Adoption of Proven "More Actions" Architecture:** Replaced `DropDownButton` + `DXPopupMenu` with the exact control architecture used by `More Actions`: `DevExpress.XtraEditors.SimpleButton` paired with `System.Windows.Forms.ContextMenuStrip` and `ToolStripMenuItem`.
    - **Single-Line Menu Activation:** Removed all artificial debounce timers, manual `ShowDropDown()` invocations, dual `Click`/`ArrowButtonClick` handlers, and `ActAsDropDown` flags. Menu display is handled purely by:
      `_operationsMenu.Show(_operationsButton, new Point(0, _operationsButton.Height));`
    - **Visual Prominence & Hierarchy:** `_operationsButton` retains distinctive CBOS Primary Teal (`#0D9488`), crisp bold white text (`Operations ▼`), and hover/pressed states (`#0F766E` / `#115E59`).
    - **Compact Professional Vector Glyph:** Added a subtle vector SVG gear icon (`svgimages/setup/properties.svg`) sized explicitly to `new Size(14, 14)` unscaled with `LeftCenter` alignment, `ImageToTextIndent = 5`, and `AllowGlyphSkinning = DefaultBoolean.True` (rendering pure white on teal). This avoids high-DPI scaling bloat, keeping the glyph subservient to the text. `More ▼` button preserved in neutral white/slate styling (`#475569`).
    - **Preserved Domain Guarantees:** Single-window lifecycle (`CbosApplicationContext`), Back Office permission gating (`menu.dashboard`), cart protection (`PosNavigationGuardDialog`), and extension point isolation (`Refund / Return` hidden until fully functional).

20. **Restaurant POS Shift, Cash Drawer & Day-Close Operations (2026-09-23):**
    - **Architectural Shift & Cash Drawer Lifecycle:** Implemented complete cashier shift management and business day close operations natively integrated into CBOS POS and Back Office workflows.
    - **POS Entry Gate (`PosEntryGateCoordinator`):** Opening POS from Back Office navigation, module dispatch, or document activation runs through the entry gate. The gate inspects active shifts for the authenticated cashier and terminal. If an active shift exists, POS opens directly with that shift active. If no active shift exists, it presents the `OpenShiftDialog`. If the cashier enters starting cash and confirms, the shift is opened via `OpenShiftCommand` and POS opens. If cancelled, POS entry is aborted and the user remains safely in Back Office.
    - **Header Operations Command Menu (`Operations ▼`):** Fully operational launcher utilizing `DevExpress.XtraEditors.SimpleButton` paired with `System.Windows.Forms.ContextMenuStrip`:
      - **`Shift` submenu:**
        - `Open Shift`: Opens a new shift if none is active; disabled when a shift is active.
        - `Current Shift / Shift Details`: Displays active shift ID, cashier name, terminal ID, opening time, starting float, running cash sales, paid-ins, paid-outs, and running expected cash in drawer.
        - `Close Shift`: Initiates the shift-closing blind cash count workflow. Checks for working uncompleted orders and prompts cashier to complete or hold before proceeding. Closes shift via `CloseShiftCommand` and automatically transitions to Back Office.
      - **`Cash Movement`:** Provides `CashMovementDialog` for recording Pay-In and Pay-Out cash movements against the active shift with mandatory positive amount, selected category/reason, and cashier notes. Dispatches `RecordCashMovementCommand`.
      - **`Print Last Receipt`:** Automatically looks up the most recently completed order for the current shift and terminal via `GetLastCompletedOrderForShiftQuery` and invokes `ReceiptPrintPreviewDialog` with complete tender breakdown and audit identifiers. Disabled when no completed order exists.
      - **`End of Day`:** Permission-gated manager workflow (`endofday.close`). Invokes `EndOfDayCloseDialog`.
      - **`Back Office`:** Permission-gated navigation command (`menu.dashboard`) returning to Back Office with transient cart guard protection (`PosNavigationGuardDialog`).
    - **Blind Cash Count & Expected Cash Formula:** Shift closing employs blind reconciliation where expected cash is not displayed to the cashier during physical counting. Expected cash is strictly calculated in domain:
      $$\text{ExpectedCash} = \text{StartingCash} + \text{CashPayments} + \text{PayIns} - \text{PayOuts} - \text{CashRefunds}$$
      $$\text{Variance} = \text{CountedCash} - \text{ExpectedCash}$$
    - **Business Day Close Invariants:**
      - Explicit business-date awareness decoupled from shift closing: multiple shifts per business date are supported across multiple terminals and cashiers.
      - Day Close is an atomic manager operation executing `CloseBusinessDayCommand`.
      - Strict precondition: Day Close is strictly blocked if ANY terminal shift for that business date is still open (`HasOpenShifts == true`), displaying a warning banner and disabling the Confirm Day Close button.
      - Generates immutable `BusinessDayClose` aggregate record storing aggregated sales, tender totals, paid-ins/outs, and net variance.
    - **High-DPI & Single-Window Integrity:** Dialogs and menu controls utilize `DesktopDpi.Scale`, vector SVGs, and adhere strictly to `IApplicationModeNavigator` and `CbosApplicationContext` single-window lifetime guarantees.

21. **POS Shift Auto-Resume & Deterministic Terminal Identity Correction (2026-09-23):**
    - **Canonical Terminal Resolution Engine (`TerminalResolutionService`):**
      Resolves the physical workstation's terminal identity deterministically across sessions without relying on hardcoded dummy GUIDs. Evaluates in strict precedence:
      1. `CBOS_TERMINAL_ID` environment variable (UUID, short Code, or Display Name).
      2. Persisted workstation store (`PosSettingsStore.LoadTerminalId()`) validated against active master data (auto-clears stale/deleted entries).
      3. Host machine hostname (`Environment.MachineName`) matched case-insensitively against active terminals.
      4. Single active branch terminal auto-binding.
      5. Deterministic fallback to first active terminal ordered alphabetically by Code (`OrderBy(t => t.Code.Value)`).
    - **POS Shift Entry Gate Decision Matrix (`PosShiftAccessService`):**
      - **Case A (Same User + Same Terminal + Open Shift):** Immediate auto-resume with 0 additional clicks, preserving `ShiftId`, `OpenedAtUtc`, `StartingCash`, `CashMovements`, and running drawer balances.
      - **Case B (No Open Shift):** Displays `OpenShiftDialog` to initiate a new cashier float session.
      - **Case C (Different User + Same Terminal + Open Shift):** Blocks entry with dialog `"Terminal Shift Already Open"`, indicating occupying cashier name and shift number.
      - **Case D (Same User + Different Terminal + Open Shift):** Blocks entry with informative dialog `"Active Shift on Another Terminal"`, indicating existing terminal name/code and current workstation terminal name/code.
    - **Self-Healing Persistence Migration:**
      `RestaurantPersistenceInitializer` automatically migrates legacy shifts with placeholder GUIDs (`00000000-0000-0000-0000-000000000001`) to the branch's canonical active terminal on application startup, guaranteeing zero database-level mismatch or orphaned shift state.

22. **BackOffice-to-POS Mode Transition & UI-Thread Lifetime Invariants (2026-09-24):**
    - **Defect Resolution:** Resolved the release-blocking defect where transitioning from Back Office (`MainForm`) to Restaurant POS caused a frozen white screen and automatic window closure after 15–20 seconds.
    - **Root Cause & Fix:** Asynchronous shift-access queries in `EnsureShiftAndOpenPosAsync` resumed on ThreadPool worker threads via `.ConfigureAwait(false)`. Due to early resolution of `ApplicationModeNavigator` before `WindowsFormsSynchronizationContext` was set on the UI thread, `_uiSyncContext` was null, causing `OpenPosCoreAsync` to instantiate and show `RestaurantPosForm` on the worker thread without a Windows message pump.
    - **Architectural Solution:**
      - `Program.cs` registers `WindowsFormsSynchronizationContext` immediately after initialization and passes it to `ApplicationModeNavigator`.
      - `ApplicationModeNavigator.MarshalToUiThreadAsync` guarantees that `OpenPosAsync` and `OpenBackOfficeAsync` marshal execution to the primary STA UI thread (`_uiThreadId == 1`) using `SynchronizationContext.Post`, with fallbacks to `_currentForm.BeginInvoke` and `Application.OpenForms[0].BeginInvoke`.
      - `CbosApplicationContext` enforces that closing `previousForm` during mode transitions never terminates the application message loop (`if (sender != null && MainForm != null && !ReferenceEquals(sender, MainForm)) return;`).
      - `RestaurantPosForm` tracks close intent (`PosCloseInitiator`), ensuring that only explicit user closure of the window ('X' button) triggers application exit, while mode switches (Shift Close, Back Office) return seamlessly to `MainForm`.

23. **Live Testing Status:** READY FOR USER MANUAL RETEST (LIVE UI = NOT EXECUTED).

See [Restaurant POS Architecture](../docs/architecture/RestaurantPOSArchitecture.md) for complete technical architecture, lifecycle contracts, and verification suites.



